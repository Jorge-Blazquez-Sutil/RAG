[project]
name = "rag-assistant"
version = "0.1.0"
description = "Asistente RAG de Nicho — backend FastAPI y prototipo Streamlit"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "python-multipart>=0.0.12",
    "anthropic>=0.40",
    # Ingesta — extracción de texto (licencias BSD / MIT)
    "pypdf>=5.1",
    "python-docx>=1.1",
    # Ingesta — segmentación (MIT); tiktoken cuenta tokens del modelo de embeddings
    "langchain-text-splitters>=0.3",
    "tiktoken>=0.8",
    # Ingesta — embeddings
    "openai>=1.57",
]

# Cada rama de módulo añade aquí sus dependencias:
#   ingestion  -> sentence-transformers (proveedor de embeddings local)
#   storage    -> chromadb
#   retrieval  -> sentence-transformers (cross-encoder)
#   ui         -> streamlit

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "ruff>=0.8",
    "pre-commit>=4.0",
    # Genera los PDFs de prueba en tiempo de test (no se versiona ningún binario)
    "reportlab>=4.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend/app"]

# ---------- Ruff ----------
[tool.ruff]
line-length = 100
target-version = "py312"
# Para que `app.*` se ordene como paquete de primera parte, no como dependencia externa.
src = ["backend", "tests"]
extend-exclude = [".venv", "data", "chroma_db"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "SIM", "RUF"]
ignore = ["B008"]  # Depends() en firmas de FastAPI

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]

# ---------- Pytest ----------
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend"]
asyncio_mode = "auto"
addopts = "-q"
